apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
spec:
  hostnames:
  - {{ .Values.domain }}
  parentRefs:
  - name: crc-gateway
    namespace: default
    sectionName: https
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /prometheus/
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: /
    backendRefs:
    - name: kube-prometheus
      port: 9090
  - matches:
    - path:
        type: Exact
        value: /prometheus
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplaceFullPath
          replaceFullPath: /
    backendRefs:
    - name: kube-prometheus
      port: 9090
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: prometheus-auth
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: prometheus
  extAuth:
    {{- if .Values.prom_auth_backend.contextExtensions }}
    contextExtensions:
    {{- range $k, $v := .Values.prom_auth_backend.contextExtensions }}
    - name: {{ $k }}
      value: {{ $v | quote }}
    {{- end }}
    {{- end }}
    headersToExtAuth:
    {{- toYaml .Values.authProxy.authHeaders | nindent 4 }}
    {{- if eq (default "http" .Values.prom_auth_backend.protocol) "grpc" }}
    grpc:
      backendRefs:
      - name: {{ .Values.prom_auth_backend.service }}
        {{- if .Values.prom_auth_backend.namespace }}
        namespace: {{ .Values.prom_auth_backend.namespace }}
        {{- end }}
        port: {{ .Values.prom_auth_backend.port }}
    {{- else }}
    http:
      path: {{ .Values.prom_auth_backend.path }}
      backendRefs:
      - name: {{ .Values.prom_auth_backend.service }}
        {{- if .Values.prom_auth_backend.namespace }}
        namespace: {{ .Values.prom_auth_backend.namespace }}
        {{- end }}
        port: {{ .Values.prom_auth_backend.port }}
    {{- end }}
