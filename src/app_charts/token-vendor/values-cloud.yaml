token_vendor:
  allow_any_method: true
  resources:
    requests:
      memory: "50Mi"
      cpu: "100m"

authProxy:
  authHeaders:
  - authorization
  - cookie
  - x-forwarded-access-token
  - x-forwarded-host
  - apikey-token
  - x-server-name

