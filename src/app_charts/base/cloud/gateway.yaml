{{- if eq .Values.use_gateway "true" }}
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: crc-gateway-proxy-config
  namespace: envoy-gateway-system
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyService:
        type: LoadBalancer
        externalTrafficPolicy: Local
        loadBalancerIP: {{ .Values.ingress_ip }}
      envoyDeployment:
        pod:
          affinity:
            podAntiAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 100
                podAffinityTerm:
                  labelSelector:
                    matchLabels:
                      app.kubernetes.io/name: envoy
                  topologyKey: kubernetes.io/hostname
        container:
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 2000m
              memory: 2Gi
  shutdown:
    drainTimeout: 60s
    minDrainDuration: 15s
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
  parametersRef:
    group: gateway.envoyproxy.io
    kind: EnvoyProxy
    name: crc-gateway-proxy-config
    namespace: envoy-gateway-system
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: crc-gateway
spec:
  gatewayClassName: eg
  listeners:
  - name: http
    # hostname: {{ .Values.domain }} # Uncomment to restrict this listener to the configured domain. Required if multiple Gateways share the same IP.
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All
  - name: https
    # hostname: {{ .Values.domain }} # Uncomment to restrict this listener to the configured domain. Required if multiple Gateways share the same IP (via SNI).
    protocol: HTTPS
    port: 443
    tls:
      mode: Terminate
      certificateRefs:
      - name: tls
        kind: Secret
    allowedRoutes:
      namespaces:
        from: All
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-redirect
spec:
  parentRefs:
  - name: crc-gateway
    sectionName: http
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: crc-gateway-client-traffic-policy
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: crc-gateway
  tls:
    minVersion: "1.2"
    alpnProtocols:
    - h2
    - http/1.1
  # Maintain Chunk (1 MiB) < Stream Window (2 MiB) < Connection Window (4 MiB) < Buffer Limit (5 MiB)
  # so HTTP/2 flow control regulates high-BDP streams within the Linux kernel TCP receive buffer
  # ceiling (~4.5 MiB) without triggering Envoy's per-connection high-watermark socket brake.
  connection:
    bufferLimit: 5Mi
  http2:
    initialStreamWindowSize: 2097152      # 2 MiB
    initialConnectionWindowSize: 4194304  # 4 MiB
    connectionKeepalive:
      idleInterval: 60s
      interval: 60s                       # Sends periodic HTTP/2 PING frames to prevent L4 load balancer 600s idle drops
      timeout: 15s
  timeout:
    http:
      streamIdleTimeout: 3600s            # Raise per-stream idle timeout from 5m default to 1h for long-running/idle streams
---
# Align with NGINX default behavior: NGINX has no total request duration cap,
# whereas Envoy Gateway defaults to a 15s requestTimeout. Setting requestTimeout
# to 0s disables the 15s cap and allows infinite total request duration by default.
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: crc-gateway-backend-traffic-policy
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: crc-gateway
  connection:
    bufferLimit: 5Mi
  http2:
    initialStreamWindowSize: 1048576      # 1 MiB (overrides 64 KiB default to prevent 16x WINDOW_UPDATE stalls on 1 MiB chunks)
    initialConnectionWindowSize: 4194304  # 4 MiB
    connectionKeepalive:
      idleInterval: 60s
      interval: 60s
      timeout: 15s
  timeout:
    http:
      requestTimeout: 0s
      streamIdleTimeout: 3600s
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: envoy-proxy-metrics
  namespace: envoy-gateway-system
  labels:
    prometheus: kube-prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: envoy
      app.kubernetes.io/component: proxy
  podMetricsEndpoints:
    - port: metrics
      path: /stats/prometheus
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_node_name]
          targetLabel: instance
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: envoy-gateway-metrics
  namespace: envoy-gateway-system
  labels:
    prometheus: kube-prometheus
spec:
  selector:
    matchLabels:
      control-plane: envoy-gateway
  endpoints:
    - port: metrics
      path: /metrics
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-dashboards-json
  namespace: envoy-gateway-system
  labels:
    grafana: "1"
data:
  envoy-proxy-global.json: |-
{{ .Files.Get "files/envoy-proxy-global.json" | indent 4 }}
  envoy-gateway-global.json: |-
{{ .Files.Get "files/envoy-gateway-global.json" | indent 4 }}
  envoy-clusters.json: |-
{{ .Files.Get "files/envoy-clusters.json" | indent 4 }}
{{- end }}


