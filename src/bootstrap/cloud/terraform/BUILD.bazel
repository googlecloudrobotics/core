filegroup(
    name = "terraform",
    srcs = [
        "config.sh.tmpl",
        "www.yaml",
    ] + glob(
        include = ["*.tf"],
        exclude = ["backend.tf"],
    ),
    visibility = ["//visibility:public"],
)
